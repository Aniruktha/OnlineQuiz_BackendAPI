version: '3.9'

services:
  db:
    image: postgres
    container_name: quiz_postgres
    restart: always
    environment:
      POSTGRES_DB: quizdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Aniruktha@123
    ports:
      - "5433:5432"

  redis:
    image: redis
    container_name: quiz_redis
    ports:
      - "6379:6379"